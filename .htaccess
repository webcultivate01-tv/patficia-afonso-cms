RewriteEngine On

# Serve index.html for the root
RewriteRule ^$ index.html [L]

# Pass everything else (non-file, non-dir) to index.php
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^(.*)$ index.php [QSA,L]
